<%- include("./partials/header") %>

<h1>LIST OF PROFILES</h1>

<% users.forEach(user => { %>

    <div class="user">
        <p><a href="/<%= user.username %>/profile"><%= user.username.toUpperCase() %></a></p>
    </div>

<% }) %>

<%- include("./partials/footer") %>